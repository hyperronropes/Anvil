"""DEPRECATED — use prepare_logo.py (runs from logo_source.png).

This procedural fallback is kept only for devs with no logo asset.
"""
import sys
print("Use prepare_logo.py instead (build.bat calls that).", file=sys.stderr)
sys.exit(1)
